root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/azadi ./cmd/server"
  bin = "tmp/azadi"
  include_ext = ["go", "html"]
  exclude_dir = ["tmp", "frontend", "node_modules", ".git"]
  delay = 1000

[misc]
  clean_on_exit = true
